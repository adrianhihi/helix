import type { FailureClassification, RepairCandidate } from '../../core/types.js';

// PLACEHOLDER — Stripe repair strategies
// Coming soon

export function stripeConstruct(_failure: FailureClassification): RepairCandidate[] {
  return [];
}
